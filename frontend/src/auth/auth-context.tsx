/* eslint-disable react-refresh/only-export-components */
// Compatibility module for active Vite clients that loaded the original path.
export { AuthContext, useAuth, useHasPermission } from "./auth-state";
export type { AuthContextValue } from "./auth-state";
